# FIX: Undefined symbol: _main

## Проблема
```
Undefined symbols for architecture arm64: "_main"
```

Xcode не может найти точку входа в приложение (`@main`). Это значит, что `FocusGardenApp.swift` не добавлен в компиляцию.

---

## ✅ РЕШЕНИЕ 1: Добавить файл в Compile Sources

### Шаги:

1. **Выберите проект** (синяя иконка FocusGarden вверху Navigator)

2. **Выберите Target "FocusGarden"** (под TARGETS)

3. **Перейдите на вкладку "Build Phases"**

4. **Разверните "Compile Sources"** (нажмите на треугольник)

5. **Проверьте, есть ли `FocusGardenApp.swift` в списке:**
   - ✅ Если есть - переходите к Решению 2
   - ❌ Если нет - нажмите кнопку **"+"** внизу списка

6. **Найдите `FocusGardenApp.swift`** в диалоге

7. **Нажмите "Add"**

8. **Clean и Build:**
   - `Shift + Cmd + K` (Clean)
   - `Cmd + B` (Build)

---

## ✅ РЕШЕНИЕ 2: Проверить Target Membership

### Если файл УЖЕ в Compile Sources:

1. **В Project Navigator** (слева) найдите `FocusGardenApp.swift`

2. **Выберите файл** (один клик)

3. **Откройте File Inspector** (правая панель, иконка документа)

4. **Найдите секцию "Target Membership"**

5. **Убедитесь, что стоит галочка** ✅ рядом с "FocusGarden"

6. Если галочки нет:
   - Поставьте галочку
   - Clean и Build

---

## ✅ РЕШЕНИЕ 3: Проверить все файлы

Убедитесь, что ВСЕ Swift файлы добавлены в Target:

### В Build Phases → Compile Sources должны быть:

```
✅ FocusGardenApp.swift
✅ ContentView.swift
✅ TimerView.swift
✅ GardenView.swift
✅ SettingsView.swift
✅ StatisticsView.swift
✅ ProgressTrackerView.swift
✅ TimerSettings.swift
✅ Progress.swift
✅ DailyRecord.swift
✅ AppViewModel.swift
✅ TimerViewModel.swift
✅ StorageManager.swift
✅ SoundManager.swift
✅ Extensions.swift
```

### Если каких-то файлов нет:

1. Нажмите "+" в Compile Sources
2. Добавьте недостающие файлы
3. Clean и Build

---

## ✅ РЕШЕНИЕ 4: Nuclear Option

Если ничего не помогает:

### Удалите все файлы и добавьте заново:

1. **В Project Navigator:**
   - Выберите все Swift файлы в проекте
   - Right-click → Delete
   - Выберите **"Remove Reference"** (НЕ "Move to Trash"!)

2. **Добавьте файлы обратно:**
   - В Finder откройте `FocusGarden/FocusGarden/FocusGarden/`
   - Перетащите все папки (Models, Views, ViewModels, Managers, Helpers) и файл FocusGardenApp.swift в Xcode
   - При запросе выберите:
     - ✅ Copy items if needed
     - ✅ Create groups
     - ✅ Add to targets: FocusGarden

3. **Clean и Build:**
   ```
   Shift + Cmd + K
   Cmd + B
   ```

---

## 🔍 Проверка Build Phases

После любого решения, проверьте что у вас есть:

### Build Phases должен содержать:

1. **Dependencies** (может быть пустым)
2. **Compile Sources** (все .swift файлы)
3. **Link Binary With Libraries** (пустой или системные фреймворки)
4. **Embed Frameworks** (обычно пустой для простых проектов)

### НЕ должно быть:

- ❌ Info.plist в Copy Bundle Resources
- ❌ Дубликатов файлов в Compile Sources
- ❌ .swift файлов в Copy Bundle Resources

---

## ⚠️ О предупреждениях фреймворков

Эти предупреждения можно игнорировать:
```
Could not find or use auto-linked framework 'CoreAudioTypes'
Could not find or use auto-linked framework 'UIUtilities'
```

Они не критичны для iOS 26.1 simulator.

---

## 🎯 Быстрая проверка

### Команда в терминале:

```bash
cd ~/Downloads/Swift/FocusGarden/FocusGarden
find . -name "FocusGardenApp.swift"
```

Должна вернуть:
```
./FocusGarden/FocusGardenApp.swift
```

---

## ✅ После исправления

Вы должны увидеть:
- Build Succeeded ✅
- Приложение запускается
- Нет ошибки "_main"

---

## 💡 Почему это происходит?

Swift приложения требуют точку входа - файл с `@main`.
Если Xcode не компилирует `FocusGardenApp.swift`, он не может
создать исполняемый файл.

Обычно это происходит когда:
1. Файл не добавлен в Target
2. Файл случайно исключен из компиляции
3. Проект был создан неправильно

---

**Попробуйте Решение 1 сначала - обычно это самое быстрое!** ⚡
